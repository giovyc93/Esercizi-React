import React from "react";
import Sum from ".Sum/"


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
  <Sum a={1} b={2}/>
  </React.StrictMode>
);

